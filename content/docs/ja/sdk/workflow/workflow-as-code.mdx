---
title: Workflow-as-Code
description: defineWorkflow() を使用して TypeScript でワークフローを定義し、プラットフォームに同期します — バージョン管理された通知ロジック。
---

通知ワークフローをアプリケーションコードと一緒に定義し、単一のコマンドまたは API 呼び出しで Nexus に同期させることができます。

## なぜ Workflow-as-Code なのか

| ダッシュボードのみ         | Workflow-as-Code                                         |
| :------------------------- | :------------------------------------------------------- |
| キャンバス上での手動編集   | Git で追跡可能な TypeScript 定義                         |
| PR での差分レビューが困難  | 通常のコードと同様にワークフローの変更差分をレビュー可能 |
| コンパイル時チェックがない | IDE によるステップタイプのオートコンプリート（自動補完） |

## ワークフローの定義

```ts
import { defineWorkflow } from '@nexus-signal/node';

export const orderShipped = defineWorkflow('order.shipped', {
  description: 'Notify when an order ships',
  tags: ['orders', 'transactional'],
  active: true,
})
  .addChannel('IN_APP')
  .addChannel('EMAIL', { suppressWhenOnline: true })
  .addDelay({ hours: 2 })
  .addChannel('SMS', {
    conditions: {
      logic: 'AND',
      rules: [{ variable: 'data.priority', operator: 'equals', value: 'high' }],
    },
  })
  .toDefinition();
```

## プラットフォームへの同期

```ts
import { NexusClient } from '@nexus-signal/node';
import { orderShipped } from './workflows/order-shipped';

const nexus = new NexusClient({ secretKey: process.env.NEXUS_SECRET_KEY! });

const result = await nexus.workflows.sync(orderShipped);
// 複数同期させる場合: await nexus.workflows.sync([orderShipped, welcomeEmail]);
```

同等の HTTP リクエスト：

```http
POST /v1/workflows/sync
Authorization: Bearer sk_prd_…
```

## 利用可能なステップビルダー

| メソッド                                     | キャンバスノードへのマッピング    |
| :------------------------------------------- | :-------------------------------- |
| `addChannel(kind, opts?)`                    | Channel (EMAIL, SMS, IN_APP など) |
| `addDelay({ hours, minutes })`               | Delay                             |
| `addDigest({ windowMinutes })`               | Digest                            |
| `addThrottle({ maxCount, windowSeconds })`   | Throttle                          |
| `addHttpFetch({ url, method, responseKey })` | HTTP Fetch                        |
| `addAiNode({ prompt, responseKey })`         | AI Node                           |

すべてのステップメソッドは、オプションの `conditions`（条件）を受け入れます。詳細は [ステップの条件](/docs/platform/features/step-conditions) を参照してください。

## 高度な例

```ts
export const escalation = defineWorkflow('payment.overdue')
  .addHttpFetch({
    url: 'https://api.billing.com/invoices/{{ data.invoiceId }}',
    method: 'GET',
    responseKey: 'invoice',
  })
  .addAiNode({
    prompt:
      'Classify urgency for invoice {{ data.invoice.amount }} due {{ data.invoice.dueDate }}. Return: low, medium, or high.',
    responseKey: 'urgency',
  })
  .addChannel('EMAIL')
  .addChannel('SMS', {
    conditions: {
      logic: 'OR',
      rules: [{ variable: 'data.urgency', operator: 'equals', value: 'high' }],
    },
  })
  .toDefinition();
```

## CLI 同期

```bash
nexus workflows sync --file ./workflows/order-shipped.json
# または、プルされた JSON を .nexus/workflows/ からアップロードします
nexus workflows push --all
```

## MCP 同期

AI エージェントは `nexus_sync_workflows` ツールを介して定義をプッシュできます。詳細は [MCP サーバー](/docs/sdk/devtools/mcp-server) を参照してください。

## 関連リンク

- [ワークフローのコンセプト](/docs/platform/concepts/workflows)
- [CLI](/docs/sdk/devtools/cli)
- [HTTP Fetch ノード](/docs/platform/features/http-fetch-node)
- [AI ワークフローノード](/docs/platform/features/ai-workflow-node)
