---
title: API Reference
description: REST API overview for Nexus Signal — base URL, auth, and endpoint index.
---

**Base URL:** `https://api.nexussignal.dev/v1`

## Authentication modes

| Audience | Auth | Endpoints |
|----------|------|-----------|
| Server apps | `Authorization: Bearer sk_*` | `/workflows/trigger`, `/schedules/sdk` |
| Browser SDK | `x-nexus-public-key: pk_*` | `/sdk/*` |
| Dashboard | JWT session cookie / Bearer | `/workflows/dashboard`, `/analytics`, etc. |

## Response conventions

- Triggers return **202 Accepted** (async processing)
- Errors return `{ error: string, code?: string, details?: object }`
- Rate limits apply per plan tier

## Endpoint index

| Section | Description |
|---------|-------------|
| [Workflows](/docs/api-reference/workflows) | Trigger API |
| [SDK](/docs/api-reference/sdk) | Inbox, preferences, topics |
| [Schedules](/docs/api-reference/schedules) | Scheduled triggers |
| [Analytics](/docs/api-reference/analytics) | Metrics and cost |

## Health

```http
GET /health
GET /health/deep
```

## Telemetry (public)

```http
GET /v1/telemetry/click/:trackingId
POST /v1/telemetry/callbacks/:provider
```

Click tracking redirects users and logs engagement. Carrier callbacks update delivery status.
