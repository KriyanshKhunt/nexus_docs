---
title: SDK React
description: "@nexus-signal/react — campana de notificaciones, inbox y matriz de preferencias."
---

Componentes React listos para usar en notificaciones in-app y preferencias de suscriptor.

## Instalación

<Tabs items={['npm', 'pnpm', 'yarn']}>
  <Tab value="npm">

```bash
npm install @nexus-signal/react socket.io-client
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm add @nexus-signal/react socket.io-client
```

  </Tab>
  <Tab value="yarn">

```bash
yarn add @nexus-signal/react socket.io-client
```

  </Tab>
</Tabs>

## NexusProvider

Envuelve tu app (o layout autenticado):

```tsx title="app/layout.tsx"
import { NexusProvider } from "@nexus-signal/react";

export function AppShell({ user, hmac }: { user: User; hmac: string }) {
  return (
    <NexusProvider
      publicKey={process.env.NEXT_PUBLIC_NEXUS_PUBLIC_KEY!}
      subscriberId={user.id}
      hmacSignature={hmac}
      backendUrl="https://api.nexussignal.dev"
    >
      {children}
    </NexusProvider>
  );
}
```

<TypeTable
  type={{
    publicKey: {
      type: "string",
      description:
        "Clave pública del entorno (comienza con pk_dev_, pk_prd_, etc.). Obligatoria.",
    },
    subscriberId: {
      type: "string",
      description:
        "El mismo subscriber externalId único usado en tus triggers de flujo. Obligatorio.",
    },
    hmacSignature: {
      type: "string",
      description:
        "La firma HMAC generada en tu backend para seguridad. Obligatoria.",
    },
    backendUrl: {
      type: "string",
      default: "https://api.nexussignal.dev",
      description:
        "El origen base del endpoint de la API de la plataforma Nexus (también mapeable mediante la propiedad apiBaseUrl).",
    },
  }}
/>

## Campana de notificaciones

```tsx title="components/header.tsx"
import { NotificationCenterBell } from "@nexus-signal/react";

<header>
  <NotificationCenterBell position="bottom-end" variant="ghost" />
</header>;
```

- Actualizaciones en vivo vía WebSocket con badge de no leídos
- Acciones de marcar como leído y archivar
- Sincronización de lectura entre dispositivos

## Inbox de notificaciones

Feed de página completa con agrupación por fecha e infinite scroll:

```tsx title="pages/notifications.tsx"
import { NotificationInbox } from "@nexus-signal/react";

<NotificationInbox />;
```

Usa `GET /v1/sdk/notifications` con paginación (`hasMore`, `loadMore`).

## Matriz de preferencias

```tsx title="pages/settings.tsx"
import { SubscriberPreferenceMatrix } from "@nexus-signal/react";

<SubscriberPreferenceMatrix variant="borderless" groupByTopic />;
```

Renderiza eventos de flujo o **topics** frente a checkboxes de canal. Los cambios persisten vía la API de preferencias del SDK.

## Hook useNotifications

```tsx title="components/notification-list.tsx"
import { useNotifications } from "@nexus-signal/react";

const { items, unread, status, loadMore, hasMore, markRead, markAllRead } =
  useNotifications();
```

<TypeTable
  type={{
    items: {
      type: "InAppNotification[]",
      description:
        "Array de objetos de notificación in-app obtenidos actualmente.",
    },
    unread: {
      type: "number",
      description: "Contador de notificaciones no leídas y no archivadas.",
    },
    status: {
      type: "'connected' | 'disconnected'",
      description: "Estado actual de la conexión WebSocket en tiempo real.",
    },
    hasMore: {
      type: "boolean",
      description: "Indica si hay más notificaciones históricas por cargar.",
    },
    loadingMore: {
      type: "boolean",
      description:
        "Flag que indica si hay una solicitud de obtención de página actualmente en curso.",
    },
    loadMore: {
      type: "() => Promise<void>",
      description:
        "Obtiene la siguiente página de notificaciones y las añade al feed de items.",
    },
    markRead: {
      type: "(logId: string) => Promise<void>",
      description: "Marca una notificación específica como leída.",
    },
    markAllRead: {
      type: "() => Promise<void>",
      description: "Marca todas las notificaciones actuales como leídas.",
    },
    archive: {
      type: "(logId: string) => Promise<void>",
      description:
        "Archiva una notificación específica, marcándola como leída y eliminándola de la lista unread activa.",
    },
    remove: {
      type: "(logId: string) => Promise<void>",
      description:
        "Elimina una notificación por completo de la base de datos y del feed.",
    },
  }}
/>

## Tematización

Los componentes usan variables CSS (`--nexus-theme-*`) compatibles con Tailwind. Sobrescribe en tu CSS global para adaptarlos a tu marca.

## Seguridad

<Callout type="error">
  Genera `hmacSignature` en tu servidor para cada sesión. Nunca pongas
  `secretKey` en código del frontend.
</Callout>
